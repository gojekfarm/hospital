groups:
- name: queue_build_up
  rules:
  - alert: queue_exceed
    expr: haproxy_backend_current_queue{backend="localnodes",instance="localhost:9101",job="haproxy"} > 50
    for: 1s
    labels:
      severity: page      
    annotations:
      summary: Current queue is greater than 100
